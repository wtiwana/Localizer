{
  "name": "@localizer/demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "vercel-build": "cd ../.. && npm run build -w localizer && npm run build -w @localizer/demo",
    "postbuild": "rm -rf ../../dist && cp -r dist ../../dist",
    "preview": "vite preview",
    "preview:self-hosted": "vite preview --open '?mode=self-hosted'"
  },
  "dependencies": {
    "localizer": "*"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
