{
  "name": "@localizer/demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "rm -rf ../../dist && cp -r dist ../../dist",
    "preview": "vite preview",
    "preview:self-hosted": "vite preview --open '?mode=self-hosted'"
  },
  "dependencies": {
    "localizer": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.2.5"
  }
}
